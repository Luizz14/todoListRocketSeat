import * as React from 'react';
import { styles } from "./styles";
import { View } from 'react-native';

export default function Header() {
    return (
        <View>
            <View style={styles.divTop}></View>
        </View>
    )
}